# deps / build
node_modules/
.next/
out/
build/
tsconfig.tsbuildinfo
# env & secrets — NEVER commit
.env
.env.*
!.env.example
# .env.production holds ONLY placeholder bootstrap values (no real secrets) so
# the Vercel deploy runs; real secrets go in the Vercel dashboard, which wins.
!.env.production
# misc
.DS_Store
*.log
.vercel
next-env.d.ts
# playwright e2e artifacts
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
